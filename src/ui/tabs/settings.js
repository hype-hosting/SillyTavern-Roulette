/* SillyTavern-Roulette — AGPL-3.0
 * Settings tab — animation speed + accent override. Step 9 build target.
 */

export function mountSettingsTab(container) {
    container.innerHTML = `
        <div class="roulette-tab-placeholder">
            <i class="fa-solid fa-sliders"></i>
            <p>Settings — coming in step 9.</p>
        </div>
    `;
}

export function refreshSettingsTab(_container) {
    // no-op for step 1
}
